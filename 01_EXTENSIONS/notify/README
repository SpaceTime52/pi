
  에이전트 작업 완료 시 터미널 네이티브 알림을 보낸다.
  알림 제목은 고정된 한국어 fallback("작업 완료")을 사용하고, 본문은 한국어 단일 한 줄 summary를 사용한다.
  세션명은 알림 제목/본문에 다시 넣지 않는다.
  모델 호출이 불가능하거나 적절한 요약을 만들 수 없으면 본문은 비운다.

  SUPPORTED
    OSC 777    Ghostty, iTerm2, WezTerm, rxvt-unicode
    OSC 99     Kitty
