
  에이전트 작업 완료 시 터미널 네이티브 알림을 보낸다.
  auto-session-title가 소스 영역에 세션명을 이미 노출하므로, 알림 제목은 한국어 단일 한 줄 summary를 우선 사용한다.
  본문은 비워서 중복 노출을 줄인다.
  모델 호출이 불가능하거나 적절한 요약을 만들 수 없으면 제목은 세션명(없으면 π)으로 fallback한다.

  SUPPORTED
    OSC 777    Ghostty, iTerm2, WezTerm, rxvt-unicode
    OSC 99     Kitty
