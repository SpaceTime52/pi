
  에이전트 작업 완료 시 터미널 네이티브 알림을 보낸다.
  제목은 세션명(없으면 π), 본문은 현재 모델이 만든 한국어 단일 한 줄 summary를 사용한다.
  여러 bullet/문장이 있어도 합치지 않고 첫 번째 핵심 결과만 요약한다.
  모델 호출이 불가능하거나 한국어 summary를 만들 수 없으면 본문은 한국어 fallback("작업 완료")를 사용한다.

  SUPPORTED
    OSC 777    Ghostty, iTerm2, WezTerm, rxvt-unicode
    OSC 99     Kitty
