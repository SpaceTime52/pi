pre-commit:
  commands:
    format:
      glob: "*.ts"
      run: mise exec -- biome format --write {staged_files} && git add {staged_files}
    lint:
      run: mise exec -- npm run lint
    typecheck:
      run: mise exec -- npm run typecheck
    test:
      run: mise exec -- npm test
